[
  // =====================================================
  // CUSTOM BINDINGS (Scheme A -- Minimal)
  // =====================================================
  // 17 unique custom bindings (26 entries across contexts).
  //
  // Modifier categories:
  //
  //   secondary- (platform-adaptive: Ctrl on Linux, Cmd on macOS)
  //     - 1 dock toggle (Secondary+?)
  //     - 1 left dock / explorer (Secondary+Shift+E), Workspace + Editor
  //     - 1 copy path (Secondary+Shift+C)
  //     - 1 open file under cursor (Secondary+Enter)
  //
  //   ctrl- (fixed Ctrl on ALL platforms, including macOS)
  //     - 2 pane navigation (Ctrl+H, Ctrl+L), Workspace + Editor
  //     - 2 jump list (Ctrl+O, Ctrl+I), Workspace + Editor
  //     - 1 close tab (Ctrl+Q), Workspace + Editor
  //     - 1 Claude Code launcher (Ctrl+Shift+A), Workspace + Terminal
  //     - 2 indent/outdent (Ctrl+>, Ctrl+<), Editor + null at Workspace
  //
  //   alt- (fixed Alt on all platforms)
  //     - 1 vim toggle (Alt+V), Workspace
  //     - 2 line movers (Alt+J, Alt+K)
  //     - 1 reload file from disk (Alt+R)
  //     - 1 build PDF (Alt+Shift+E) -- Typst/Slidev dispatcher
  //     - 1 preview in browser (Alt+Shift+P) -- Typst/Slidev dispatcher
  //
  // The ctrl- bindings intentionally remain Ctrl (not Cmd) on
  // macOS to avoid collisions with Zed defaults (e.g. Cmd+Q
  // quits the app, Cmd+H hides it). See ZED DEFAULT REFERENCE
  // below for the macOS defaults this config leaves intact.

  // Pane navigation (move focus between split panes)
  // Jump list: Ctrl+O / Ctrl+I (vim-style back/forward without vim mode)
  // Vim toggle: Alt+V
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "secondary-?": "workspace::ToggleRightDock",
      "secondary-shift-e": "workspace::ToggleLeftDock",
      "ctrl-shift-a": ["task::Spawn", { "task_name": "Claude Code" }],
      "ctrl-q": "pane::CloseActiveItem",
      "ctrl-o": "pane::GoBack",
      "ctrl-i": "pane::GoForward",
      "alt-v": "workspace::ToggleVimMode"
    }
  },

  // Claude Code from terminal context
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-shift-a": ["task::Spawn", { "task_name": "Claude Code" }]
    }
  },

  // Editor-context overrides
  //
  // Ctrl+H and Ctrl+L are re-declared here so that Zed's default
  // Editor-context Ctrl+H (Find and replace in file) does not
  // shadow the Workspace-context pane-navigation binding above.
  // Without these overrides, pressing Ctrl+H inside an editor
  // opens find/replace instead of activating the left pane.
  {
    "context": "Editor",
    "bindings": {
      "alt-j": "editor::MoveLineDown",
      "alt-k": "editor::MoveLineUp",
      "ctrl-q": "pane::CloseActiveItem",
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "ctrl-o": "pane::GoBack",
      "ctrl-i": "pane::GoForward",
      "secondary-shift-c": "workspace::CopyPath",
      "secondary-shift-e": "workspace::ToggleLeftDock",
      "alt-shift-e": ["task::Spawn", { "task_name": "Build PDF" }],
      "alt-shift-p": ["task::Spawn", { "task_name": "Preview in Browser" }],
      "secondary-enter": "editor::OpenFile",
      "alt-r": "editor::ReloadFile",
      "ctrl->": "editor::Indent",
      "ctrl-<": "editor::Outdent"
    }
  },

  // Defeat Zed's default ctrl-> (agent::AddSelectionToThread) which
  // is bound in Editor&&mode==full, AgentPanel, AcpThread>Editor,
  // and Terminal contexts.  Null it out everywhere, then let the
  // Editor-context bindings above provide indent/outdent.
  {
    "context": "Workspace",
    "bindings": {
      "ctrl->": null,
      "ctrl-<": null
    }
  },
  {
    "context": "Editor && mode == full",
    "bindings": {
      "ctrl->": "editor::Indent",
      "ctrl-<": "editor::Outdent"
    }
  },

  // Project panel hjkl navigation (vim-style without vim mode)
  //
  // Provides h/j/k/l movement inside the file explorer while
  // leaving vim mode disabled globally. Guarded by `not_editing`
  // so the inline rename input (F2) still receives literal keys.
  //   h -- collapse current directory (or move to parent)
  //   j -- select next entry
  //   k -- select previous entry
  //   l -- open file, or expand/collapse directory
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "h": "project_panel::CollapseSelectedEntry",
      "j": "menu::SelectNext",
      "k": "menu::SelectPrevious",
      "l": "project_panel::Open"
    }
  }

  // =====================================================
  // ZED DEFAULT REFERENCE (macOS)
  // =====================================================
  // These are built-in Zed shortcuts on macOS -- no config
  // needed. Listed here for quick reference. Entries marked
  // (custom) are NOT Zed defaults; they are rebindings from
  // the CUSTOM BINDINGS block above and are listed here so
  // the exception is visible alongside the default table.
  // Entries marked (verify) are best-guess macOS defaults
  // that should be confirmed against Zed's keymap docs
  // before relying on them.
  //
  // --- File Operations ---
  // Cmd+P             Open file by name (file finder)
  // Cmd+S             Save file
  // Cmd+Shift+S       Save all files (verify)
  // Cmd+N             New file
  // Cmd+W             Close active tab
  // Cmd+Shift+T       Reopen closed tab
  //
  // --- Navigation ---
  // Ctrl+Tab          Next tab (platform-agnostic in Zed)
  // Ctrl+Shift+Tab    Previous tab (platform-agnostic in Zed)
  // Ctrl+G            Go to line number
  // Cmd+Shift+O       Go to symbol in file
  // F12               Go to definition
  // Ctrl+O            Go back in jump list (custom; vim-style)
  // Ctrl+I            Go forward in jump list (custom; vim-style)
  // Ctrl+-            Go back (previous location) (verify)
  // Ctrl+Shift+-      Go forward (next location) (verify)
  //
  // --- Panels & Sidebars ---
  // Cmd+B             Toggle left sidebar
  // Secondary+Shift+E File explorer / toggle left dock (custom; overrides project_panel::ToggleFocus on macOS)
  // Cmd+Shift+G       Git panel (verify)
  // Ctrl+`            Toggle terminal (Ctrl+ on macOS too)
  // Cmd+Shift+M       Problems panel (verify)
  //
  // --- Search ---
  // Cmd+F             Find in file
  // Cmd+Alt+F         Find and replace in file (verify)
  // Cmd+Shift+F       Search across all files
  // Cmd+Shift+H       Replace across all files (verify)
  //
  // --- Agent / AI ---
  // Ctrl+Shift+A      Launch Claude Code CLI (custom; terminal task)
  // Secondary+?       Toggle right dock (custom; secondary- = Ctrl on Linux, Cmd on macOS)
  // Cmd+?             Toggle agent panel (verify; overridden by custom binding on macOS)
  // Cmd+N             New conversation (in agent panel) (verify)
  // Cmd+Enter         Send message (in agent panel) (verify)
  // Cmd+;             Toggle inline assist (verify)
  //
  // --- Navigation ---
  // Secondary+Enter   Open file under cursor (custom; secondary- = Ctrl on Linux, Cmd on macOS)
  //
  // --- Build / Preview (Typst + Slidev) ---
  // Alt+Shift+E         Build PDF (custom; dispatches by file extension)
  // Alt+Shift+P         Preview in Browser (custom; dispatches by file extension)
  //
  // --- Mode ---
  // Alt+V             Toggle vim mode (custom)
  //
  // --- Editing ---
  // Cmd+Z             Undo
  // Cmd+Shift+Z       Redo
  // Cmd+C             Copy
  // Cmd+X             Cut
  // Cmd+V             Paste
  // Cmd+D             Select next occurrence
  // Ctrl+Shift+K      Delete line (verify; may be Cmd+Shift+K)
  // Cmd+/             Toggle comment
  // Cmd+]             Indent line
  // Cmd+[             Outdent line
  // Cmd+Alt+Up        Add cursor above (verify)
  // Cmd+Alt+Down      Add cursor below (verify)
  // Secondary+Shift+C Copy path (custom; secondary- = Ctrl on Linux, Cmd on macOS)
  //
  // --- Code Actions ---
  // Cmd+.             Quick fix / code actions
  // F2                Rename symbol
  // Ctrl+Space        Trigger autocomplete
  // Cmd+Shift+I       Format document (verify)
  //
  // --- Pane Navigation ---
  // Ctrl+H            ActivatePaneLeft (custom; see Workspace bindings)
  // Ctrl+L            ActivatePaneRight (custom; see Workspace bindings)
  //
  // --- Git ---
  // Alt+G B           Toggle git blame
  //
  // --- Markdown ---
  // Cmd+K V           Markdown preview (side-by-side) (verify)
  // Cmd+Shift+V       Markdown preview (full tab) (verify)
  //
  // --- Window Management ---
  // Cmd+\             Split pane right (verify)
  // Cmd+Shift+\       Split pane down (verify)
  // Cmd+Shift+P       Command palette
  // Cmd+,             Open settings
]
