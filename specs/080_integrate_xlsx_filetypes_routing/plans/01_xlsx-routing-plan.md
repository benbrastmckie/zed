# Implementation Plan: Task #80

- **Task**: 80 - integrate_xlsx_filetypes_routing
- **Status**: [NOT STARTED]
- **Effort**: 1 hour
- **Dependencies**: Task 79 (completed)
- **Research Inputs**: specs/080_integrate_xlsx_filetypes_routing/reports/01_xlsx-routing-audit.md
- **Artifacts**: plans/01_xlsx-routing-plan.md (this file)
- **Standards**: plan-format.md, status-markers.md, artifact-management.md, tasks.md
- **Type**: meta
- **Lean Intent**: false

## Overview

Task 79 created skill-xlsx, xlsx-agent, and the /xlsx command directly in the zed project but never placed the source files in the extension directory at `~/.config/nvim/.claude/extensions/filetypes/`. The source manifest.json, EXTENSION.md, and index-entries.json already reference xlsx, but the actual files are missing from the source tree. This plan copies the three files to the source extension directory, re-runs the extension loader to regenerate extensions.json, context/index.json, and CLAUDE.md, and clarifies routing in skill-filetypes.

### Research Integration

The research report (01_xlsx-routing-audit.md) identified:
- Three files exist only in the zed project, not in the source extension directory
- The source manifest.json already declares xlsx-agent, skill-xlsx, and xlsx.md
- extensions.json, context/index.json, and CLAUDE.md are auto-generated by the loader and will update automatically once source files are in place
- The filetypes-router-agent routes xlsx only to spreadsheet-agent (table conversion), not to xlsx-agent for create/edit/analyze operations
- convert.md already supports xlsx-to-markdown (no changes needed)

### Prior Plan Reference

No prior plan.

### Roadmap Alignment

No active roadmap items. The ROADMAP.md contains no items relevant to this task.

## Goals & Non-Goals

**Goals**:
- Copy xlsx source files (skill, agent, command) to the source extension directory so re-syncs and other projects get xlsx support
- Re-run the extension loader to properly register xlsx in extensions.json, context/index.json, and CLAUDE.md
- Clarify routing: xlsx creation/editing/analysis goes to skill-xlsx, xlsx table extraction goes to skill-filetypes-spreadsheet

**Non-Goals**:
- Modifying the xlsx-agent, skill-xlsx, or xlsx.md content (already functional from task 79)
- Changing convert.md (xlsx-to-markdown conversion already works)
- Adding new xlsx features or MCP server configuration (openpyxl MCP already declared in manifest)

## Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Extension loader fails on re-sync | M | L | Source manifest already declares xlsx; loader will find files after copy |
| Existing non-symlink files in zed project conflict with loader | M | M | Back up existing files, remove them, let loader re-install from source |
| skill-filetypes routing edit introduces ambiguity | L | L | Use clear, documented routing distinction between table extraction and full xlsx ops |

## Implementation Phases

**Dependency Analysis**:
| Wave | Phases | Blocked by |
|------|--------|------------|
| 1 | 1 | -- |
| 2 | 2 | 1 |
| 3 | 3 | 2 |

Phases within the same wave can execute in parallel.

### Phase 1: Copy Source Files to Extension Directory [NOT STARTED]

**Goal**: Place the three xlsx files in the source extension directory so the loader can install them properly.

**Tasks**:
- [ ] Create `skills/skill-xlsx/` directory in `~/.config/nvim/.claude/extensions/filetypes/`
- [ ] Copy `~/.config/zed/.claude/skills/skill-xlsx/SKILL.md` to `~/.config/nvim/.claude/extensions/filetypes/skills/skill-xlsx/SKILL.md`
- [ ] Copy `~/.config/zed/.claude/agents/xlsx-agent.md` to `~/.config/nvim/.claude/extensions/filetypes/agents/xlsx-agent.md`
- [ ] Copy `~/.config/zed/.claude/commands/xlsx.md` to `~/.config/nvim/.claude/extensions/filetypes/commands/xlsx.md`
- [ ] Verify all three files exist in the source extension directory

**Timing**: 10 minutes

**Depends on**: none

**Files to modify**:
- `~/.config/nvim/.claude/extensions/filetypes/skills/skill-xlsx/SKILL.md` - new file (copy)
- `~/.config/nvim/.claude/extensions/filetypes/agents/xlsx-agent.md` - new file (copy)
- `~/.config/nvim/.claude/extensions/filetypes/commands/xlsx.md` - new file (copy)

**Verification**:
- `ls ~/.config/nvim/.claude/extensions/filetypes/skills/skill-xlsx/SKILL.md` exists
- `ls ~/.config/nvim/.claude/extensions/filetypes/agents/xlsx-agent.md` exists
- `ls ~/.config/nvim/.claude/extensions/filetypes/commands/xlsx.md` exists
- Manifest provides entries match filesystem: `jq '.provides.agents[]' manifest.json` includes xlsx-agent.md, etc.

---

### Phase 2: Re-run Extension Loader [NOT STARTED]

**Goal**: Regenerate extensions.json, context/index.json, and CLAUDE.md to include xlsx entries.

**Tasks**:
- [ ] Remove existing non-symlink xlsx files from zed project (skill-xlsx dir, xlsx-agent.md, xlsx.md command) to avoid loader conflict warnings
- [ ] Run the extension loader/installer to reinstall the filetypes extension from the updated source: `bash .claude/scripts/install-extension.sh .claude/extensions/filetypes` or equivalent re-sync mechanism
- [ ] If install-extension.sh requires a local `.claude/extensions/filetypes` directory (symlink or copy), create it pointing to the source
- [ ] Verify extensions.json now includes `skill-xlsx` in `installed_dirs`, `xlsx-agent.md` in `installed_files`, and `xlsx.md` in `installed_files`
- [ ] Verify context/index.json now contains xlsx-agent load_when entries from index-entries.json
- [ ] Verify CLAUDE.md Filetypes Extension section now lists skill-xlsx and /xlsx

**Timing**: 30 minutes

**Depends on**: 1

**Files to modify**:
- `.claude/extensions.json` - auto-generated: add xlsx entries to filetypes
- `.claude/context/index.json` - auto-generated: merge xlsx-agent index entries
- `.claude/CLAUDE.md` - auto-generated: add skill-xlsx and /xlsx to Filetypes section
- `.claude/skills/skill-xlsx/SKILL.md` - replaced by loader-installed copy
- `.claude/agents/xlsx-agent.md` - replaced by loader-installed copy
- `.claude/commands/xlsx.md` - replaced by loader-installed copy

**Verification**:
- `jq '.extensions.filetypes.installed_dirs' .claude/extensions.json` includes `skill-xlsx`
- `jq '.extensions.filetypes.installed_files' .claude/extensions.json` includes xlsx-agent.md, xlsx.md, skill-xlsx/SKILL.md
- `grep -q 'skill-xlsx' .claude/CLAUDE.md` returns success
- `grep -q '/xlsx' .claude/CLAUDE.md` returns success
- `jq '.entries[] | select(.path | contains("xlsx"))' .claude/context/index.json` returns entries

---

### Phase 3: Clarify Routing in skill-filetypes [NOT STARTED]

**Goal**: Update skill-filetypes SKILL.md to explicitly document the routing distinction between xlsx table extraction and full xlsx operations.

**Tasks**:
- [ ] Edit `skill-filetypes/SKILL.md` (in the source extension directory) to add a note that xlsx creation/editing/analysis routes to skill-xlsx, not through filetypes-router-agent
- [ ] Add skill-xlsx to the "When NOT to trigger" section with clear routing guidance
- [ ] Re-sync the updated SKILL.md to the zed project (or rely on the loader having already installed it)

**Timing**: 15 minutes

**Depends on**: 2

**Files to modify**:
- `~/.config/nvim/.claude/extensions/filetypes/skills/skill-filetypes/SKILL.md` - add xlsx routing note
- `.claude/skills/skill-filetypes/SKILL.md` - synced copy

**Verification**:
- `grep -q 'skill-xlsx' ~/.config/nvim/.claude/extensions/filetypes/skills/skill-filetypes/SKILL.md` returns success
- The SKILL.md clearly states that xlsx create/edit/analyze goes to skill-xlsx while xlsx table extraction goes to skill-filetypes-spreadsheet

## Testing & Validation

- [ ] `/xlsx` command is accessible and routes to skill-xlsx -> xlsx-agent
- [ ] `/convert file.xlsx` still routes through filetypes-router-agent for content extraction
- [ ] `/table file.xlsx` still routes through skill-filetypes-spreadsheet
- [ ] `/edit file.xlsx "instruction"` routes to skill-xlsx -> xlsx-agent
- [ ] extensions.json filetypes entry includes all xlsx artifacts
- [ ] context/index.json contains xlsx-agent load_when entries
- [ ] CLAUDE.md Filetypes Extension section lists skill-xlsx, xlsx-agent, and /xlsx

## Artifacts & Outputs

- `specs/080_integrate_xlsx_filetypes_routing/plans/01_xlsx-routing-plan.md` (this plan)
- `specs/080_integrate_xlsx_filetypes_routing/summaries/01_xlsx-routing-summary.md` (post-implementation)
- Updated source files in `~/.config/nvim/.claude/extensions/filetypes/` (3 new files)
- Regenerated `.claude/extensions.json`, `.claude/context/index.json`, `.claude/CLAUDE.md`

## Rollback/Contingency

If the extension loader fails or produces broken output:
1. Restore extensions.json, context/index.json, and CLAUDE.md from git (`git checkout -- .claude/extensions.json .claude/context/index.json .claude/CLAUDE.md`)
2. Manually re-add the three xlsx files to the zed project `.claude/` directories (restoring pre-loader state)
3. Remove the copied files from the source extension directory if they cause issues

If routing becomes ambiguous after Phase 3:
1. Revert the skill-filetypes SKILL.md change in the source directory
2. Re-sync to the zed project
